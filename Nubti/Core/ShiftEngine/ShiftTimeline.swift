import Foundation

/// ShiftTimeline
/// 📅 ناتج حساب نظام النوبات لفترة زمنية محددة
struct ShiftTimeline {

    /// جميع الأيام المحسوبة بالترتيب الزمني
    let items: [Item]

    /// عنصر واحد يمثل يومًا واحدًا في التقويم
    struct Item: Identifiable {

        /// معرف فريد (للاستخدام في SwiftUI)
        let id = UUID()

        /// تاريخ اليوم
        let date: Date

        /// مرحلة اليوم داخل دورة النظام
        let phase: ShiftPhase

        /// هل يُحسب هذا اليوم كدوام؟
        /// (تم التعديل هنا لتقرأ من المسمى الجديد في ShiftPhase)
        var isWorkingDay: Bool {
            phase.isCountedAsWorkDay
        }
    }
}
